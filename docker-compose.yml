services:
  newscore-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: newscore-api
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://newscore:${DB_PASSWORD:-newscore123}@postgres:5432/newscoredb?schema=public
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_PASSWORD=${REDIS_PASSWORD:-}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - uploads_data:/app/uploads
    networks:
      - newscore_network
      - routy-traefik_web
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=routy-traefik_web"
      # CORS Headers Middleware - Dynamic origin based on request
      - "traefik.http.middlewares.cors-headers.headers.accessControlAllowMethods=GET,OPTIONS,PUT,POST,DELETE,PATCH,HEAD"
      - "traefik.http.middlewares.cors-headers.headers.accessControlAllowOriginList=*"
      - "traefik.http.middlewares.cors-headers.headers.accessControlAllowCredentials=true"
      - "traefik.http.middlewares.cors-headers.headers.accessControlAllowHeaders=Content-Type,Authorization,Accept,X-Requested-With"
      - "traefik.http.middlewares.cors-headers.headers.accessControlMaxAge=100"
      - "traefik.http.middlewares.cors-headers.headers.addVaryHeader=true"
      # HTTP to HTTPS redirect
      - "traefik.http.routers.newscore-api-web.rule=Host(`api.sahara2797.com`)"
      - "traefik.http.routers.newscore-api-web.entrypoints=web"
      - "traefik.http.routers.newscore-api-web.middlewares=https-redirect"
      # HTTPS
      - "traefik.http.routers.newscore-api.rule=Host(`api.sahara2797.com`)"
      - "traefik.http.routers.newscore-api.entrypoints=websecure"
      - "traefik.http.routers.newscore-api.tls.certresolver=lehttp"
      - "traefik.http.routers.newscore-api.middlewares=cors-headers"
      - "traefik.http.routers.newscore-api.service=newscore-api"
      - "traefik.http.services.newscore-api.loadbalancer.server.port=3000"
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  postgres:
    image: postgres:16-alpine
    container_name: newscore-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-newscore}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-newscore123}
      POSTGRES_DB: ${DB_NAME:-newscoredb}
    ports:
      - "127.0.0.1:5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-newscore}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - newscore_network

  redis:
    image: redis:7-alpine
    container_name: newscore-redis
    restart: unless-stopped
    command: redis-server --appendonly yes ${REDIS_PASSWORD:+--requirepass $REDIS_PASSWORD}
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "--raw", "incr", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - newscore_network

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  uploads_data:
    driver: local

networks:
  newscore_network:
    driver: bridge
  routy-traefik_web:
    external: true
